<UserControl x:Class="ModernUINavigationApp1.Pages.UpdatePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer Margin="3,6,-3,-6">
            <StackPanel MinWidth="200">
                <ProgressBar x:Name="pb" Height="22" Width="256"/>
                <TextBlock x:Name="label1" Text="{Binding Value, ElementName=pb, StringFormat=\{0:0\}%}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <Button x:Name="button" FontSize="10" Content="Загрузить архив" Width="108" Click="button_Click"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
